# ─── Local development (without docker-compose / nginx) ──────────────────────
# Set BACKEND_URL to your FastAPI server. Next.js will proxy /api/v1/* to it.
# Leave unset in production — nginx handles routing.
#
# BACKEND_URL=http://localhost:8000
